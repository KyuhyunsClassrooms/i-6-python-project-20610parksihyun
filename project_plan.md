# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제: 재난 구조 로봇의 최적 경로 안내 프로그램
---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.

내 설명: 재난 구조 로봇이 갈 수 있는 구역과 가기 힘든 구역을 구분하여 효율적으로 시민을 구출하는 프로그램


---

## 3. 입력-처리-출력 구조

### 입력

- 진입을 선호하는 구역의 특성 (예: "통신가능", "붕괴위험", "가스누출" 등)
- 로봇이 탐색할 최대 허용 위험도 점수 (예: 0 ~ 100점 사이, 점수가 높을수록 위험함)

### 처리


- 2차원 리스트에 저장된 구역 데이터를 반복문으로 검사하여,
  사용자가 입력한 '최대 허용 위험도'보다 숫자가 작으면서 '구역 특성' 조건에 맞는 구역들을 골라냅니다.
- 조건에 맞는 구역이 여러 개라면, 이동 소요 시간이 가장 짧은 구역(최적 경로)을 찾습니다.
- 만약 사용자가 입력한 위험도 조건이 너무 낮아서 진입할 수 있는 안전한 구역이 하나도 없다면,
  경고 메시지와 함께 다른 조건을 입력하라는 안내를 출력합니다.

### 출력

- 로봇이 안전하게 진입 가능한 추천 구역 목록
- 그중에서 가장 빠르게 접근할 수 있는 '최적 경로 구역'의 상세 정보(구역명, 위험도, 소요 시간)

---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름: sectors

sectors = [
]

def add_new_sector(sector_list):
    """사용자로부터 새로운 재난 구역 정보를 입력받아 리스트에 추가하는 함수"""
    print("\n[➕ 새로운 재난 구역 추가]")
    
    # 새로운 구역 ID 계산
    next_id = sector_list[-1][0] + 1 if len(sector_list) > 0 else 1
    
    name = input("▶️ 구역 이름을 입력하세요 (예: G-2 대피소): ")
    
    try:
        risk = int(input("▶️ 위험도 점수를 입력하세요 (0~100): "))
        time = int(input("▶️ 진입 소요 시간(분)을 입력하세요: "))
    except ValueError:
        print("❌ 위험도와 소요 시간은 숫자로 입력해야 합니다. 추가를 취소합니다.")
        return
        
    while True:
        user_condition = input("▶️ 선호하는 구역 특성을 입력하세요 (통신가능 / 붕괴위험 / 가스누출 / 안전): ")
    
    # 사용자가 입력한 값이 정해진 3개의 카테고리 안에 있는지 확인합니다.
        if user_condition in ["통신가능", "붕괴위험", "가스누출", "안전"]:
            break  # 올바르게 입력했으므로 반복문을 탈출합니다.
        else:
            print("❌ 잘못된 특성입니다. '통신가능', '붕괴위험', '가스누출', '안전' 중 하나를 정확히 입력해 주세요.")
    
    new_sector = [next_id, name, risk, time, user_condition]
    sector_list.append(new_sector)  # 깔끔하게 수정 완료!
    print(f"✅ '{name}' 구역이 성공적으로 데이터베이스에 등록되었습니다!")

### 한 행(row)의 의미

- [0: 구역 ID, 1: 구역 이름, 2: 위험도 점수(0~100), 3: 이동 소요 시간(분), 4: 구역 상태 특성]

### 각 열(column)의 의미

- 현재 추가된 구역

## 5. 함수 설계

- show_robot_status(): 현재 구조 로봇의 시스템 상태와 메뉴를 보여주는 함수
- search_safe_sectors(sector_list, max_risk, condition): 입력받은 위험도 점수와 특성 조건을 만족하는 구역들만 필터링하여
  새로운 리스트로 반환하는 핵심 알고리즘 함수
- find_best_sector(filtered_list): 필터링된 구역 중 이동 소요 시간(3번 열)이 가장 짧은 구역을 찾아 반환하는 함수
- print_report(results, best_sector): 최종 탐색 결과를 화면에 표 형태로 깔끔하게 출력하고, 조건에 맞는 구역이 없을 때 예외 메시지를 띄우는 함수

## 6. 사용할 제어구조

- 선택구조(if문, elif문)
- 반복구조(for문, while문)

### 조건문을 사용할 부분

- 내가 선택한 조건에 맞는 구역만 찾기

### 반복문을 사용할 부분

- 구역 추가나 진입 가능 구역 찾기 등 주요 기능들을 계속 쓸 수 있게 하기

## 7. 예상되는 예외 상황

- 입력되야하는 값(condition 등) 대신 다른 값을 입력하는 경우

## 8. Gemini에게 도움받고 싶은 부분

- 전체적인 뼈대
- 오류 찾기